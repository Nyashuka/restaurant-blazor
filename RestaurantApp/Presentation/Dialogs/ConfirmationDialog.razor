<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            Ви точно хочете це зробити?
        </MudText>
    </TitleContent>

    <DialogActions>
        <MudButton OnClick="Ok" Color="Color.Warning">Так</MudButton>
        <MudButton OnClick="Cancel" Color="Color.Secondary">Ні</MudButton>
    </DialogActions>

</MudDialog>
@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; } = null!;

    private void Ok() => MudDialog.Close();

    private void Cancel() => MudDialog.Cancel();
}