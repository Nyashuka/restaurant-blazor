@inject SidebarStateService _sidebarStateService

<MudAppBar Elevation="1">
    @* @if (_sidebarStateService.IsSidebarVisible)
    {
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start"
            OnClick="@((e) => DrawerToggle())" />
    } *@
    <MudLink Href="assortment/menu">
        <MudButton>
            Меню
        </MudButton>
    </MudLink>
    <MudLink Href="assortment/menu-sets">
        <MudButton>
            Набори
        </MudButton>
    </MudLink>
    <MudSpacer />
    <MudLink Href="Orders">
        <MudButton>
            Мої замовлення
        </MudButton>
    </MudLink>
    <MudLink Href="Logout">
        <MudButton>
            Вийти
        </MudButton>
    </MudLink>
</MudAppBar>

@code {
    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}